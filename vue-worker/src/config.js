//自定义的配置文件
const PREFIX_URL = "http://10.0.10.129:8888";
const config = {
    //后端api地址
    PREFIX_URL: PREFIX_URL,
    API_URL: `${PREFIX_URL}/api`,
    SWAGGER_URL: `${PREFIX_URL}/swagger-ui.html`,
};

export default config
